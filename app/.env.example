# Public (bundled into the client — safe to expose, NOT secret).
# Leave blank to run against in-app MSW mocks (no backend required).
EXPO_PUBLIC_API_BASE_URL=

# Example for a deployed dev stage:
# EXPO_PUBLIC_API_BASE_URL=https://abc123.execute-api.eu-west-1.amazonaws.com/dev
